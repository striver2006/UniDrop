# macOS 打包签名与公证配置模板
#
# 用法：复制为 .env 后填入真实值。.env 已被根 .gitignore 忽略，不会进版本库。
#
#     cp client/.env.example client/.env
#
# 打包命令是 `pnpm tauri:build`，**不是** `pnpm tauri build`。
# Tauri v2 的 CLI 不会自动读取 .env（client/ 与 client/src-tauri/ 两个位置都实测过），
# 由 package.json 里的 tauri:build 脚本显式 source 后再构建。
# 用错命令不会报错，只会静默产出未签名的包。
#
# 本文件全部留空时也能构建，产出 ad-hoc 签名的包：本机自用没问题，
# 分发给别人会被 Gatekeeper 拦下。

# ---------- 1. 代码签名（分发必需） ----------
#
# 用「Developer ID Application」那张证书，不是「Apple Development」——
# 后者只能在注册过的设备上运行，且无法公证。
#
# 查看本机可用身份：
#     security find-identity -v -p codesigning
#
# 填写时连同括号里的 Team ID 一起，与上面命令输出的引号内容逐字一致。
APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID1234)"

# ---------- 2. 公证（对外分发必需，本机自用可跳过） ----------
#
# 签名只证明「是谁做的」，公证是把产物交给 Apple 扫描后取回票据。
# 不公证时 spctl 判定为 rejected / Unnotarized Developer ID：
# 本机构建的产物没有 quarantine 属性，自己用不受影响；
# 别人下载安装则会被拦。
#
# 下面两组二选一，填完取消注释。

# 方式一：App Store Connect API Key（推荐，不会因改密码而失效）
#
# 在 App Store Connect → 用户和访问 → 集成 → 单独密钥 创建，
# .p8 文件只能下载一次，务必妥善保存。
# APPLE_API_KEY 是 Key ID，APPLE_API_ISSUER 是该页面上的 Issuer ID。
#
# APPLE_API_KEY="XXXXXXXXXX"
# APPLE_API_ISSUER="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
# 路径含空格时靠外层双引号即可，**不要**再套一层单引号——
# source 之后单引号会成为路径的一部分，notarytool 必然报文件不存在。
# APPLE_API_KEY_PATH="/absolute/path/to/AuthKey_XXXXXXXXXX.p8"

# 方式二：Apple ID + App 专用密码
#
# APPLE_PASSWORD 要在 appleid.apple.com 生成 App 专用密码，
# 不是账号登录密码。
#
# APPLE_ID="your-apple-id@example.com"
# APPLE_PASSWORD="xxxx-xxxx-xxxx-xxxx"
# APPLE_TEAM_ID="TEAMID1234"

# ---------- 关于 .dmg ----------
#
# Tauri 自己只公证 .app（流程是「公证 .app → 打包进 .dmg → 给 .dmg 签名」），
# 不会单独公证 .dmg，于是 dmg 本身仍是 Unnotarized Developer ID。
#
# 本项目用 scripts/notarize-dmg.sh 补上这一步，已挂在 tauri:build 之后自动执行，
# 无需手动操作。上面任一组凭证配齐即生效；没配则自动跳过，构建不会因此失败。
